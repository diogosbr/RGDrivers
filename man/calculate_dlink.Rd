% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_dlink.R
\name{calculate_dlink}
\alias{calculate_dlink}
\title{Calculate the D-LINK value (downstream reach order)}
\usage{
calculate_dlink(df, col_id, col_downstream, col_order)
}
\arguments{
\item{df}{Data frame containing the stream network reaches}

\item{col_id}{Name of the column with the unique reach ID (e.g. "cotrecho")}

\item{col_downstream}{Name of the column with the downstream reach ID (e.g. "nutrjus")}

\item{col_order}{Name of the column with the reach order (e.g. "link_mag", "order", etc.)}
}
\value{
The same \code{df}, with a new \code{D_LINK} column
}
\description{
Adds a \code{D_LINK} column to a table of stream reaches, representing the
order value (e.g. Link Magnitude) of the downstream reach.
}
\examples{
df <- data.frame(
  id = c(1, 2, 3),
  downstream = c(2, 3, NA),
  order = c(1, 2, 3)
)
calculate_dlink(df, col_id = "id", col_downstream = "downstream", col_order = "order")
}
