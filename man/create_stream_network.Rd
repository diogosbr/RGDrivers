% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_stream_network.R
\name{create_stream_network}
\alias{create_stream_network}
\title{Create a directed graph from stream reaches}
\usage{
create_stream_network(
  sf_reaches,
  col_id = "cotrecho",
  col_downstream = "nutrjus",
  col_length = NULL
)
}
\arguments{
\item{sf_reaches}{\code{sf} object with line geometries and ID (\code{col_id})
and downstream (\code{col_downstream}) columns}

\item{col_id}{Name of the column with the reach ID (e.g. "cotrecho")}

\item{col_downstream}{Name of the column with the downstream reach ID (e.g. "nutrjus")}

\item{col_length}{(Optional) Name of the column with line length, used as edge weight}
}
\value{
Directed \code{igraph} object
}
\description{
Builds an \code{igraph} object from an \code{sf} object of stream reaches
with ID and downstream ID columns.
}
\examples{
library(sf)
lines <- st_sfc(
  st_linestring(matrix(c(0,0, 1,1), ncol = 2, byrow = TRUE)),
  st_linestring(matrix(c(1,1, 2,2), ncol = 2, byrow = TRUE)),
  st_linestring(matrix(c(2,2, 3,3), ncol = 2, byrow = TRUE))
)

data <- data.frame(
  cotrecho = c(1, 2, 3),
  nutrjus = c(2, 3, NA),
  length = c(0.5, 1.2, 2.0)
)

sf_reaches <- st_sf(data, geometry = lines)

g <- create_stream_network(
  sf_reaches,
  col_id = "cotrecho",
  col_downstream = "nutrjus",
  col_length = "length"
)
plot(g)
}
