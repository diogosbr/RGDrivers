% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criar_rede_hidrografica.R
\name{criar_rede_hidrografica}
\alias{criar_rede_hidrografica}
\title{Cria grafo direcionado a partir de trechos fluviais}
\usage{
criar_rede_hidrografica(
  sf_trechos,
  col_id = "cotrecho",
  col_jus = "nutrjus",
  col_comprimento = NULL
)
}
\arguments{
\item{sf_trechos}{Objeto \code{sf} com geometrias lineares e colunas de ID (\code{col_id}) e jusante (\code{col_jus})}

\item{col_id}{Nome da coluna com ID do trecho (ex: "cotrecho")}

\item{col_jus}{Nome da coluna com ID do trecho jusante (ex: "nutrjus")}

\item{col_comprimento}{(Opcional) Nome da coluna com comprimento da linha, usado como peso}
}
\value{
Objeto \code{igraph} direcionado
}
\description{
A função constrói um objeto \code{igraph} a partir de um \code{sf} de trechos fluviais com colunas de ID e jusante.
}
\examples{
library(sf)
linhas <- st_sfc(
  st_linestring(matrix(c(0,0, 1,1), ncol = 2, byrow = TRUE)),
  st_linestring(matrix(c(1,1, 2,2), ncol = 2, byrow = TRUE)),
  st_linestring(matrix(c(2,2, 3,3), ncol = 2, byrow = TRUE))
)

dados <- data.frame(
  cotrecho = c(1, 2, 3),
  nutrjus = c(2, 3, NA),
  comprimento = c(0.5, 1.2, 2.0)
)

sf_trechos <- st_sf(dados, geometry = linhas)

g <- criar_rede_hidrografica(sf_trechos, col_id = "cotrecho", col_jus = "nutrjus", col_comprimento = "comprimento")
plot(g)
}
