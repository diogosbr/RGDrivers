% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_network_with_orders.R
\name{write_network_with_orders}
\alias{write_network_with_orders}
\title{Write a spatial vector with stream order columns}
\usage{
write_network_with_orders(
  original_vector,
  updated_df,
  col_id,
  additional_cols,
  output_path,
  overwrite = TRUE
)
}
\arguments{
\item{original_vector}{Original \code{SpatVector} object (stream network)}

\item{updated_df}{Data frame containing the reach IDs and the new columns}

\item{col_id}{Name of the column with the unique reach identifier (e.g. "cotrecho")}

\item{additional_cols}{Vector with the names of the columns to add to the vector}

\item{output_path}{Full path to save the file to (e.g. "output.gpkg")}

\item{overwrite}{Logical, if TRUE allows overwriting an existing file}
}
\value{
NULL (writes file to disk)
}
\description{
Inserts new columns (e.g. \code{link_mag}, \code{D_LINK}) into a
\code{SpatVector} object from an updated \code{data.frame} and writes it to
disk.
}
\examples{
# Hypothetical example
# write_network_with_orders(vector, updated_df, "id", c("link_mag", "D_LINK"), "output.gpkg")
}
